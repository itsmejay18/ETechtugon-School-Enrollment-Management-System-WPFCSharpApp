<UserControl x:Class="School_Management_System.Wpf.Views.Settings.GlobalSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="Global Settings"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}" />
            <TextBlock Margin="0,8,0,0"
                       Text="{Binding StatusMessage}"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource MutedTextBrush}" />
        </StackPanel>

        <Border Grid.Row="1"
                Padding="18"
                Background="#FFF8FBFF"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Margin="0,0,12,0">
                    <TextBlock Text="School Year"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryBrush}" />
                    <ComboBox Margin="0,6,0,0"
                              ItemsSource="{Binding AcademicYears}"
                              SelectedItem="{Binding SelectedAcademicYear, Mode=TwoWay}"
                              DisplayMemberPath="DisplayName" />
                </StackPanel>
                <StackPanel Grid.Column="1"
                            Margin="12,0,12,0">
                    <TextBlock Text="Current Semester"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryBrush}" />
                    <ComboBox Margin="0,6,0,0"
                              ItemsSource="{Binding Semesters}"
                              SelectedItem="{Binding SelectedSemester, Mode=TwoWay}"
                              DisplayMemberPath="DisplayName" />
                </StackPanel>
                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Bottom">
                    <Button Margin="0,0,10,0"
                            Style="{StaticResource NeutralButtonStyle}"
                            Command="{Binding RefreshCommand}"
                            Content="Refresh" />
                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding SaveCommand}"
                            Content="Save" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Margin="0,18,0,0"
                Padding="18"
                Background="White"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="16">
            <StackPanel>
                <TextBlock Text="Active term"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}" />
                <TextBlock Margin="0,8,0,0"
                           Text="{Binding ActiveTermText}"
                           FontSize="15"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryBrush}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
