<UserControl x:Class="School_Management_System.Wpf.Views.Settings.CurriculumExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:services="clr-namespace:School_Management_System.Wpf.Services">
    <UserControl.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="CanUserResizeRows" Value="False" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="RowHeaderWidth" Value="0" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="SelectionUnit" Value="FullRow" />
            <Setter Property="services:DataGridColumnDisplay.UseCompactColumns" Value="True" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Curriculum code explorer"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}" />
                <TextBlock Margin="0,6,0,0"
                           Text="{Binding StatusMessage}"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource MutedTextBrush}" />
            </StackPanel>

            <Button Grid.Column="1"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding RefreshCommand}"
                    Content="Refresh" />
        </Grid>

        <Border Grid.Row="1"
                Margin="0,0,0,16"
                Padding="16"
                Background="#FFF8FBFF"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Curriculum Code"
                               FontSize="12.5"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryBrush}" />
                    <TextBlock Margin="0,6,0,0"
                               Text="{Binding SelectedCurriculumCode}"
                               TextWrapping="Wrap"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextBrush}" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Margin="24,0,0,0">
                    <TextBlock Text="Curriculum Name"
                               FontSize="12.5"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryBrush}" />
                    <TextBlock Margin="0,6,0,0"
                               Text="{Binding SelectedCurriculumName}"
                               TextWrapping="Wrap"
                               Foreground="{StaticResource MutedTextBrush}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ComboBox ItemsSource="{Binding Courses}" SelectedItem="{Binding SelectedCourse, Mode=TwoWay}" DisplayMemberPath="DisplayName" Margin="0,0,10,0" />
                <ComboBox Grid.Column="1" ItemsSource="{Binding CurriculumTypes}" SelectedItem="{Binding SelectedCurriculumType, Mode=TwoWay}" Margin="0,0,10,0" />
                <ComboBox Grid.Column="2" ItemsSource="{Binding AcademicYears}" SelectedItem="{Binding SelectedAcademicYear, Mode=TwoWay}" DisplayMemberPath="DisplayName" Margin="0,0,10,0" />
                <ComboBox Grid.Column="3" ItemsSource="{Binding YearLevels}" SelectedItem="{Binding SelectedYearLevel, Mode=TwoWay}" DisplayMemberPath="DisplayName" Margin="0,0,10,0" />
                <ComboBox Grid.Column="4" ItemsSource="{Binding Semesters}" SelectedItem="{Binding SelectedSemester, Mode=TwoWay}" DisplayMemberPath="DisplayName" />
            </Grid>

            <DataGrid Grid.Row="1"
                      Margin="0,16,0,0"
                      AutoGenerateColumns="True"
                      AutoGeneratingColumn="CurriculumSubjectsGrid_OnAutoGeneratingColumn"
                      ItemsSource="{Binding CurriculumSubjects}" />
        </Grid>
    </Grid>
</UserControl>
